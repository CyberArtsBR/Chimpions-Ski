import assert from 'node:assert/strict';import {chromium} from '@playwright/test';
const browser=await chromium.launch({args:['--use-gl=angle','--use-angle=swiftshader','--enable-unsafe-swiftshader']});const page=await browser.newPage({viewport:{width:1440,height:900}});
try{await page.goto('http://127.0.0.1:4173/?test=1',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.chimpionsSki?.().ready,{timeout:30000});const state=await page.evaluate(()=>window.chimpionsSki());assert(state.catalogSize>180);assert.equal(state.mode,'menu');assert.equal(await page.getByRole('button',{name:'CHOOSE CHIMPION'}).isVisible(),true);assert.equal(await page.getByRole('button',{name:'START SKIING'}).isVisible(),true);await page.getByRole('button',{name:'START SKIING'}).click();await page.waitForFunction(()=>window.chimpionsSki?.().mode==='playing');console.log('PASS desktop browser');}finally{await browser.close()}
